node_modules/
dist/
src-tauri/target/
src-tauri/gen/
*.log
.DS_Store
Thumbs.db

# Playwright e2e 产物
test-results/
playwright-report/

# Agent 调试临时目录（探针脚本、日志、缓存等）。统一在项目内 .tmp/，
# 避开 cwd 根散落；Vite watch 也会忽略。git/rust/cargo/ESLint 默认都不
# 扫描它（未列入 include/exclude 即跳过）。
.tmp/
.tmp/**

