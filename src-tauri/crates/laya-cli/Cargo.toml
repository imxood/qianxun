[package]
name = "laya-cli"
description = "Laya ONNX 本地探针:加载静态 bundle,预测并实测延迟"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
laya-engine = { path = "../laya-engine" }
clap = { version = "4.5", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
